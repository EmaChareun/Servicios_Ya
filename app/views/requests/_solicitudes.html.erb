    <%# request.photos.each do |photo| %>
      <%#= cl_image_tag photo.key, height: 300, width: 400, crop: :fill %>
    <%# end%>

<div class="container">
  <div class="cards">
    <% requests.each do |request| %>
      <div class="card-request">
        <img src="https://res.cloudinary.com/mpdlo/image/upload/v1646918763/production/Recurso_1_g0t3ok.png" />
        <div class="card-request-infos">
          <h2><%= "#{request.title}"%></h2>
          <p> <%= "Dirección: #{request.city}" %> </p>
          <p> <%= "Detalles: #{request.detail}" %> </p>
          <%# <button class="btn-grad">Ver Detalles <span> </span>&rarr;</button>
          <div class="link-ver"> %>
          <%= link_to 'Ver más detalles', request_path(request), class: 'btn btn-request' %>
        </div>
      </div>
    <% end%>
  </div>
</div>
